/* function saludar() {
  console.log("hola", nombre);
}

saludar(); */

/* function calcular() {
  debugger;
  let primerNro = parseFloat(prompt("impresa el primer nro."));
  let segundoNro = parseFloat(prompt("ingresar el segundo nro."));

  if (!isNaN(primerNro) || !isNaN(segundoNro)) {
    let resultado = primerNro + segundoNro;
    console.log("resultado de la operacion", resultado);
  } else {
    alert("ingresar numero pendejo");
  }
}
 */
/* calcular();

function calcular(primerNro, segundoNro) {
  if (!isNaN(primerNro) || !isNaN(segundoNro)) {
    let resultado = primerNro + segundoNro;
    console.log("resultado de la operacion", resultado);
  } else {
    alert("ingresa numeros pendejo");
  }
}

function pedirNro() {
  let primerNro = parseFloat(prompt("ingresar el primer nro."));
  let segundoNro = parseFloat(prompt("ingresa el segundo nro."));
  calcular(primerNro, segundoNro);
}
 */

/* function concatenarCompleto(nombre, apellido) {
  console.log(nombre, apellido);
} */

function verProductos() {
  let productos = "productos disponibles:";
}
